# Force LF line endings for every text file regardless of platform checkout
# settings. A CRLF checkout breaks bash on the shebang line even when the
# git index stores LF — see CI's "CRLF regression check". A blanket rule means
# a new script under any path is covered without adding a per-glob pattern.
* text=auto eol=lf
*.sh text eol=lf
